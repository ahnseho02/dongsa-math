# 자료

## prototype.html

서버 없이 브라우저만으로 도는 **최초 프로토타입**이다. 고모에게 개념을 보여 주려고 만들었다.
문제 템플릿과 네 단계 풀이가 전부 이 한 파일 안에 자바스크립트로 들어 있다.

지금은 실제 앱(`src/main/resources/static`)이 이걸 대체했다.
백엔드에 붙어 있지 않으므로 여기 있는 기록은 어디에도 저장되지 않는다.
설계 의도를 다시 볼 때만 참고한다.
